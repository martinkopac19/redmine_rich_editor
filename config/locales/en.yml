en:
  re_setting_enabled: "Enable rich editor"
  re_setting_enabled_hint: "Turn the Linear-style inline editor on/off. When off, the native textarea editor is used."
  re_ph: "Write…  (\"/\" for blocks)"
  re_link_prompt: "Link (URL):"
  re_no_blocks: "No blocks"
  re_add_comment: "Add a comment"
  re_submit: "Submit"
